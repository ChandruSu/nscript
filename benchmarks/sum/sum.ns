
let std = import("std");
let t0 = std.time();
let N = std.parseInt(args[0]);

let sum = 0;
while N > 0 {
  sum += N;
  N -= 1;
}

std.println("Total sum: " + std.str(sum));

let t1 = std.time();

std.println("Execution took (ms) " + std.str(t1 - t0));