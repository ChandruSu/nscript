
fun Animal(name, species) {
    let self = {
        "name": name
    };

    self.sound = fun() {
        return "I'm " + self["name"] + ", and I am a " + species;
    };
    
    return self;
}

let john = Animal("John", "Dog");
john.sound();
